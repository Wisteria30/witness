Run a full guardrail scan for the current project, summarize findings by owner layer and violation class, and point to any pending report files.

Use the `scan` skill in forked context. Do not patch code in this command.
